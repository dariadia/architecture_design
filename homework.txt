1. Найдите в проекте паттерн Шаблонный метод (Template Method). Напишите, в
каком классе и методе он применён.
Сначала искала абстрактный класс, который бы реализовывали, чего в папке src нет. Относительно, но BaseController (абстрактный, есть метод render) и контроллеры похожи на шаблонный метод. С натяжкой относительно.
Можно было бы сделать что-нибудь из интерфейсов (другой вопрос зачем) в абстрактный класс, я покрутила, странно получается. 

2. На странице продуктов (/product/list) добавить сортировку курсов, используя
паттерн Стратегия (Strategy).
++ 

3. Переписать файл Kernel.php с использованием паттерна Команда (Command).
Каждый метод (кроме process), вызываемый из handle(), положить в отдельную
команду. Все команды положить в папку Framework/Command.
Выглядит странно, но допустим. ++